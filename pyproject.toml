[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dbridge"
version = "0.2.10"
description = ''
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
keywords = []
authors = [{ name = "realEbi", email = "ebrahim.soroush@gmail.com" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = [
  "pydantic==2.10.4",
  "pydantic-settings==2.7.0",
  "duckdb==1.1.3",
  "sqlglot>=25.0.0",
  "numpy==2.2.1",
  "pandas==2.2.3",
  "tomli-w>=1.0.0",
]

[project.scripts]
dbridge = "dbridge.server:main"

[project.optional-dependencies]
mysql = ["aiomysql>=0.3.2,<0.4", "cryptography"]
postgres = ["psycopg2"]
snowflake = ["snowflake-connector-python"]

[project.urls]
Documentation = "https://github.com/realebi/dbridge#readme"
Issues = "https://github.com/realebi/dbridge/issues"
Source = "https://github.com/realebi/dbridge"

[dependency-groups]
dev = ["ruff", "black", "isort", "pyright", "pytest", "ipython"]
test = ["coverage[toml]", "pytest", "pytest-cov", "pytest-asyncio"]
types = ["mypy>=1.0.0", "pytest-asyncio"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
# Coverage is deliberately not in addopts: a focused single-test run would then
# always fail the fail_under gate. Use `pytest --cov` for the gated run.
addopts = "-ra"

[tool.coverage.run]
# Measure the shipped server only. Test modules are excluded deliberately: a
# test file scores near 100% by construction and tells you nothing about
# dbridge. Parked adapters are excluded while they remain unregistered and
# their drivers optional. Optional remote adapters have local real-server suites;
# their loaders remain measured even when the adapter module is excluded.
source = ["src/dbridge"]
branch = true
omit = [
  "src/dbridge/__about__.py",
  "src/dbridge/adapters/_parked/*",
  "src/dbridge/adapters/mysql.py",
]

[tool.coverage.paths]
dbridge = ["src/dbridge", "*/dbridge/src/dbridge"]

[tool.coverage.report]
# The floor, not the target. The suite runs well above this; the gap is
# deliberate so an ordinary change does not trip the gate.
fail_under = 85
exclude_lines = [
  "no cov",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
  # An `@abstractmethod` with a bare `...` body holds no behavior. Covering one
  # requires instantiating an ABC, which asserts nothing about dbridge.
  "^\\s*\\.\\.\\.$",
  "@(abc\\.)?abstractmethod",
]

[tool.mypy]
# Unregistered legacy adapters need separate ports, not missing-driver ignores.
# A port moved out of _parked/ re-enters recursive type checking automatically.
exclude = ["^src/dbridge/adapters/_parked/"]
