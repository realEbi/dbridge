## 1. Make commands

- [x] 1.1 Add help-first phony `manual-prepare`, `test`, and `test-cov` targets in dbridge, with `UV` and `PYTEST_ARGS` overrides; verify help/dry runs, sample preparation and repeat preparation in a disposable copy, and the automated test targets including coverage at or above 85%.

## 2. Documentation and completion

- [x] 2.1 Update the manual/development guides and sample rebuild references; replace embedded Python with an interactive checklist and existing test commands, update guide descriptions in README/architecture, and verify commands, connection parameters, row counts, and links; record shipped verification tooling in the roadmap without completing the milestone.
- [x] 2.2 Record verification evidence, review affected document ownership, and validate OpenSpec and whitespace; confirm the tooling change has no spec deltas before archiving.
