## 1. Apply entry points

- [x] 1.1 Add repository-instruction preflight and ownership/delegation checks to all three apply entry points; verify the same policy logic while retaining each tool's invocation syntax and OpenSpec state handling.
- [x] 1.2 Align completion guidance with required, authorized repository steps and document regeneration handling; verify file links and instruction ownership remain coherent.

## 2. Verification and closure

- [x] 2.1 Validate skill metadata and independently evaluate stale-policy, nested/cross-repository, resumed-worktree, and explicit-user-constraint cases; record observed outcomes and limits.
- [x] 2.2 Validate OpenSpec and whitespace, confirm this change has not edited the original checkout and preserve any concurrent user work, and confirm no capability synchronization is needed before archiving the completed tooling change.
